import 'package:flutter/material.dart';

class Collection {
  final String? image, title, description;
  final Color? color;

  Collection({
    this.image,
    this.title,
    this.description,
    this.color,
  });
}