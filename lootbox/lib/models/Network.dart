import 'package:flutter/material.dart';

class Network {
  final String? image, title, description, url;
  final Color? color;

  Network({
    this.image,
    this.title,
    this.description,
    this.url,
    this.color,
  });
}